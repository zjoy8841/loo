# 📌 2026-05-13 세션 결과 노트 · 다음 세션 시작점

> **다음 세션 첫 메시지에 이 문서 통째로 붙여넣거나 "SESSION-2026-05-13.md 읽고 이어가줘"라고 하면 됩니다.**

---

## 🎯 다음 세션 첫 액션 (1줄로 시작)

> 이어서 작업하자. SESSION-2026-05-13.md 읽고 §"다음 작업" §1부터 진행해줘. 노션 [화면 Description](https://www.notion.so/35fc2986e14081cc9890efbd525ab46d) 인덱스가 source of truth.

---

## 🛠️ 환경 상태 (세션 재시작 후 확인)

1. **Notion MCP** — `claude mcp list`에 `notion: https://mcp.notion.com/mcp ✓ Connected` 확인. `needs auth`면 `/mcp` → notion → Authenticate.
2. **Figma MCP** — `figma-developer-mcp`에 신규 PAT 등록됨 (`File content: read` scope 포함). 작동 안 하면 `~/.claude.json` 확인 또는 본 노트 §환경 변동 참조.
3. **figma-developer-mcp 우회 방식** — 이번 세션은 MCP 프로세스가 old 토큰 캐시 문제로 curl 직접 호출로 진행. 신 세션은 MCP 정상 작동할 가능성 높음. 안 되면 §SOP의 curl fallback 사용.

---

## ✅ 이번 세션 완료 사항

**노션** (Life OS ONE_v2 인덱스 하위):
- 부모 인덱스: **[화면 Description](https://www.notion.so/35fc2986e14081cc9890efbd525ab46d)** (8개 화면 인벤토리 표 + v1 ✓)
- 9개 하위 페이지 — U-01 / U-02 공통 / U-02-1~U-02-7
- Active Policy Documents 표에 `화면 Description v1` 행 등재 완료
- 세션 종료 노트 섹션에 `SESSION-2026-05-13.md` 최신 표시

**노션 페이지 URL**:
| 화면ID | URL |
|---|---|
| U-01 | https://www.notion.so/35fc2986e140810593dcd80f2786e026 |
| U-02 공통 | https://www.notion.so/35fc2986e14081d88fbef8d8763f82d2 |
| U-02-1 | https://www.notion.so/35fc2986e1408192b124e0a13ed56469 |
| U-02-2 | https://www.notion.so/35fc2986e140817d8618ffc225ec27f7 |
| U-02-3 | https://www.notion.so/35fc2986e140814aac67f8ebaffe2356 |
| U-02-4 | https://www.notion.so/35fc2986e1408110ae35c55a5eac02ff |
| U-02-5 | https://www.notion.so/35fc2986e140811e9320dce3c64ed9d9 |
| U-02-6 | https://www.notion.so/35fc2986e140810b918dd1623291a72e |
| U-02-7 | https://www.notion.so/35fc2986e14081708d2bc44b4a3e63ae |

**git**: 본 세션 노트 SESSION-2026-05-13.md commit + push 완료.

---

## 🚀 다음 작업 (우선순위 순)

### 1. 디자이너 결정 11건 해소 → description v2 patch

사용자(=오다환)가 본인이 디자이너이므로 즉시 결정 가능. 해소 후 각 description 페이지를 `notion-update-page update_content`로 patch.

| # | 항목 | 적용 페이지 |
|---|---|---|
| 1 | **OptionCard selected state** (stroke/bg/좌측 4px border/indicator fill) — v7 Iris 가설 검증 | U-02 공통 §13, 각 단계 |
| 2 | **단계별 StepIcon SVG 6개** (현 placeholder) | U-02-2~U-02-7 |
| 3 | **에러 상태 컬러** (input 빨강 / Primary disabled 회색) | U-02-1 §6, U-02 공통 §13 |
| 4 | **Primary 버튼 색** — `#111827` vs Iris `#5B5FE0` (Splash와 통일) | U-01 §13, U-02 공통 §13 |
| 5 | **ProgressBar fill 색** — 위와 동일 | U-02 공통 |
| 6 | **"권장 안 함" 컬러 `#9ca3af`** WCAG AA 미달 강화 | U-02 공통 §12 |
| 7 | **SNS 4개 제공자** — Google/Kakao/Apple/Naver? | U-02-1 §5 |
| 8 | **약관 본문 표시** — 모달/별도 페이지/외부 링크 | U-02-1 §4 |
| 9 | **2단계 `healthy` mutex 시각 UI** (데이터-레벨로 결정 — 시각은?) | U-02-2 §3 |
| 10 | **4단계 알레르기 확인 다이얼로그 카피·시안** | U-02-4 §6 |
| 11 | **카드 라벨 2줄 wrap 정책** (`\n` 명시 vs auto wrap) | U-02-2 §6 |

### 2. 별도 description 3개 작성

이번 8개 외에 추가로 정의 필요한 화면 (시니어 리뷰에서 식별):

- **U-02-1-terms** — 약관 본문 화면 (모달/페이지). U-02-1 §4 결정 후 작성.
- **U-02-SNS** — SNS 가입 흐름 분기. password 없으므로 일괄 가입 불가 → 즉시 임시 계정 생성 + 2~7단계 인증된 PATCH. U-02-1 §5 결정 후 작성.
- **U-02-1-login** — 로그인 화면 (Splash에서 "이미 계정이 있으신가요? 로그인" 진입). 별도 화면.

### 3. A 도메인 잔여 description (P0/P1)

기획서 v5 / 인덱스 기능 인벤토리 기준 A 도메인 잔여:

- **U-03 메인** (`/user/home`) — P0. v7 6 시점 surface 적용 첫 화면. **시점 자동 전환 로직 description 필요** (06-08/10-11/12-13/14-16/19-21/22+ 6 시점).
- **U-04 알림** (`/user/notifications`) — P0.
- **U-12 마이페이지** (`/user/mypage`) — P1. 7단계 정보 수정 (기획서 v5 신규).

### 4. 자식 페이지 "공통 패턴" mention-page link patch (cosmetic, 후순위)

U-02-2~U-02-7 각 페이지의 "공통 패턴" 섹션의 mention-page link는 현재 부모 [화면 Description] 페이지를 가리킴. 실제 [U-02 공통 패턴] 페이지(`35fc2986e14081d88fbef8d8763f82d2`)를 직접 가리키도록 patch 가능.

- 영향: 작음 (부모 표 navigation 가능)
- 처리: 6개 페이지 update_content × 1회 패치

### 5. 세로 슬라이스 코딩 시작

description v2 완성 후 `web/src/app/user/` 폴더에 구현. ClaudeCode가 코딩, 사용자가 검수 (프로젝트 모드).

---

## 🔑 이번 세션이 잡은 Critical 결정 (다음 세션 그대로 유지)

이미 description에 반영됨. 향후 결정이 뒤집힐 때만 patch:

- **보안 옵션 A** — password는 React state(메모리)만, sessionStorage 미저장. 새로고침 시 1단계 redirect.
- **Idempotency-Key** — 1단계 진입 시 uuid v4 생성 → POST signup header. 서버 1시간 캐시.
- **자동 로그인 race condition 방지** — `signIn(..., { redirect: true, callbackUrl: '/user/home?from=signup' })` 사용. `redirect: false + router.replace` 금지.
- **`healthy`/`omnivore` mutex** — 데이터-레벨 (시각 자유, 저장 시 다른 옵션 무시 + 토스트).
- **`night_owl`+`early_bird` 동시 허용** — "시간대 무관" 시그널.
- **alertdialog focus trap** — Radix `<AlertDialog>` / react-aria 채택.
- **회원가입은 v7 6 시점 surface 미적용** — bg `#ffffff` 고정. 메인부터 적용 시작.

---

## 🛠️ SOP (확정)

화면 하나당:

1. **Figma fetch** — `mcp__figma-developer-mcp__get_figma_data`. MCP 캐시/scope 문제 시 fallback:
   ```bash
   TOKEN=$(node -e "console.log(JSON.parse(require('fs').readFileSync(process.env.USERPROFILE + '/.claude.json','utf8')).mcpServers['figma-developer-mcp'].args.find(a => a.startsWith('--figma-api-key=')).split('=')[1])")
   curl -s -H "X-Figma-Token: $TOKEN" "https://api.figma.com/v1/files/7gay0y94AxMz4Q82dMSdI7/nodes?ids=<NODE>" -o /tmp/figma-cache/node-<NODE>.json
   ```
   파싱 helper: `/tmp/figma-cache/inspect.js`, `scan-content.js`, `walk-signup.js` (재시작 시 소실 — 필요 시 본 노트 git 이력에서 재현).

2. **초안 작성** — 개발자 시점: 메타·목적·레이아웃·컴포넌트·인터랙션·상태·API·검증·다국어·접근성·v7 정합성·테스트.

3. **시니어 dev Agent 리뷰** — `Agent subagent_type=general-purpose` 페르소나:
   > Next.js 14 App Router + NextAuth v5 + next-intl + react-hook-form/zod + Tailwind 시니어. 디자인 시스템·i18n·a11y·보안 정통.

   요청 6개 포커스: 누락된 구현 정보·모호한 표현·잘못된 가정·i18n 변동·v7 정합성·접근성·테스트.

4. **v2 보완** — Agent critical fix 모두 반영.

5. **노션 저장** — `notion-create-pages`. markdown 표 자동 → HTML 변환됨 (테스트 완료).
   - ⚠️ `options.diet` 같은 도메인-like 텍스트는 노션이 자동 링크화. `options.diet` → `options\.diet`로 escape 필요할 수 있음.

6. **부모 표 갱신** — `notion-update-page update_content`. 화면 인벤토리 표의 해당 행 patch + 상태 ✓.

회원가입 7단계는 **공통 패턴 1개 + 단계별 차이점 7개** 모델 (이미 검증됨).

---

## 📊 환경 변동 (이 세션에서 처리)

1. **Notion MCP 서버 등록 + OAuth 인증** — `claude mcp add -s user --transport http notion https://mcp.notion.com/mcp` + `/mcp` Authenticate.
2. **Figma 토큰 scope 보강** — 기존 토큰은 `file_content:read` 권한 없음 → Figma Security 페이지에서 신규 PAT 발급 (File content read 포함). `claude mcp remove/add`로 등록 갱신. 토큰 값은 `~/.claude.json`에 박혀 있음.
3. **figma-developer-mcp 우회 — curl + Node.js parser** — 이 세션은 MCP 프로세스가 old 토큰을 캐시하고 있어, Bash로 `https://api.figma.com/v1/files/{key}/nodes?ids={id}` 직접 호출. 다음 세션은 새 MCP 프로세스라 정상 작동 예상.

---

## 🔗 활성 참조 (다음 세션 fetch)

- **인덱스**: <https://www.notion.so/35dc2986e14081a2b4b9dd2b1ddcc486> (Life OS ONE_v2)
- **화면 Description 부모**: <https://www.notion.so/35fc2986e14081cc9890efbd525ab46d>
- **기획서 v5**: <https://www.notion.so/35ec2986e1408111972bd8a7b16c8c0c>
- **유저 플로우 v1**: <https://www.notion.so/35ec2986e140819fb2f3cbfc36318f41>
- **디자인 가이드 v7**: <https://www.notion.so/35ec2986e140817eae13e458c3d21fa4> · 상세는 [GitHub Pages](https://zjoy8841.github.io/loo/docs/design-guide/design-guide.html)
- **Figma 파일**: fileKey `7gay0y94AxMz4Q82dMSdI7` (LIFE OS ONE_사용자 APP)
- **U-03 메인 노드ID**: 미확인 — Figma에서 시점별 변형 6개 확인 필요 (또는 1개 노드로 시점 variant)

---

## 🗑️ 임시 자산 (재시작 시 소실, 필요시 재현)

세션 재시작 시 `/tmp/figma-cache/`는 사라집니다:
- `node-{nodeId}.json` — Figma raw dump (curl 재호출로 재현)
- `inspect.js`, `scan-content.js`, `walk-signup.js` — parser scripts (필요 시 재작성 — 본 노트 git 이력 참조)
- `draft-*.md`, `v2-*.md` — description draft (노션이 source of truth이므로 불필요)

---

## ⚠️ 다음 세션이 주의할 점

- 노션 markdown 표는 자동 HTML 변환되지만, `options.diet` 같은 텍스트가 자동 링크화되는 부작용 있음. URL/도메인-like 텍스트는 `\` escape 또는 `` ` `` 인라인 코드로.
- 자식 페이지의 "공통 패턴" mention-page link가 부모 Description 페이지를 가리킴 (정확히는 U-02 공통 패턴 페이지를 가리켜야). 시간 되면 patch.
- 디자이너 결정 항목을 일괄 해소 후 v2 patch가 효율적. 한 번에 보낼지 사용자에게 확인 권장.
- 노션 mention-page URL은 hyphen 없는 32자 hex. 공백 들어가면 깨짐 (이번 세션에 한 번 발생, 즉시 수정).
