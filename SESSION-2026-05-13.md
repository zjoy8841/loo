# 📌 2026-05-13 세션 결과 노트

> 본 세션은 시작점 노트(이전 버전)에 적은 작업을 그대로 완료하고 그 결과로 본 노트를 update한 것. 이전 시작점 내용은 본 세션에서 모두 수행되었으므로 더 이상 보존할 필요 없음.

---

## 🎯 한 줄 요약

Figma 사용자 앱 8개 화면(Splash + 회원가입 7단계) **개발자용 description** 작성 → 노션 **`화면 Description`** 인덱스 + 9개 하위 페이지로 저장. 시니어 dev Agent 리뷰 2회로 보안·race condition·idempotency·a11y critical 결함 잡고 v2 반영.

---

## ✅ 결과 (노션 페이지)

부모 인덱스: **[화면 Description](https://www.notion.so/35fc2986e14081cc9890efbd525ab46d)** (Life OS ONE_v2 하위)

| 화면ID | 페이지 |
|---|---|
| U-01 | [U-01 스플래쉬 (랜딩)](https://www.notion.so/35fc2986e140810593dcd80f2786e026) |
| U-02 | [U-02 회원가입 공통 패턴](https://www.notion.so/35fc2986e14081d88fbef8d8763f82d2) |
| U-02-1 | [U-02-1 회원가입 — 계정](https://www.notion.so/35fc2986e1408192b124e0a13ed56469) |
| U-02-2 | [U-02-2 회원가입 — 건강 상태](https://www.notion.so/35fc2986e140817d8618ffc225ec27f7) |
| U-02-3 | [U-02-3 회원가입 — 체형 목표](https://www.notion.so/35fc2986e140814aac67f8ebaffe2356) |
| U-02-4 | [U-02-4 회원가입 — 식이 / 알레르기](https://www.notion.so/35fc2986e1408110ae35c55a5eac02ff) |
| U-02-5 | [U-02-5 회원가입 — 라이프스타일](https://www.notion.so/35fc2986e140811e9320dce3c64ed9d9) |
| U-02-6 | [U-02-6 회원가입 — 직업](https://www.notion.so/35fc2986e140810b918dd1623291a72e) |
| U-02-7 | [U-02-7 회원가입 — 관심사](https://www.notion.so/35fc2986e14081708d2bc44b4a3e63ae) |

---

## 🛠️ 진행 SOP (확정)

화면 하나당:
1. Figma fetch (curl + Node.js parser — MCP 우회. §환경 변동 참조)
2. 텍스트 초안 작성 (개발자 시점: 목적·레이아웃·컴포넌트·인터랙션·상태·API·접근성·다국어·v7 토큰)
3. 시니어 dev Agent 리뷰 (subagent_type=general-purpose, Next.js 14 + NextAuth v5 + next-intl + a11y 페르소나)
4. 초안 보완 (v2)
5. 노션 저장 (markdown table 자동 → HTML 변환됨)

회원가입 7단계는 **공통 패턴 1개 + 단계별 차이점 7개** 모델 — 중복 없이 효율적.

---

## 📊 환경 변동 (이 세션에서 처리)

1. **Notion MCP 서버 등록 + OAuth 인증** — 이전 세션 미해결 항목. `claude mcp add -s user --transport http notion https://mcp.notion.com/mcp` + `/mcp` Authenticate.
2. **Figma 토큰 scope 보강** — 기존 토큰은 `file_content:read` 권한 없음 → Figma Security 페이지에서 신규 PAT 발급 (File content read 포함). `claude mcp remove/add`로 등록 갱신 (Claude Code 재시작 없이 curl 직접 호출로 우회 진행). 토큰 값은 본 노트에 미게재 — `~/.claude.json` 또는 Figma 설정 페이지에서 확인.
3. **figma-developer-mcp 우회 — curl + Node.js parser** — 현 세션 MCP 프로세스가 old 토큰을 캐시하고 있어, Bash로 `https://api.figma.com/v1/files/{key}/nodes?ids={id}` 직접 호출. `/tmp/figma-cache/` 임시 폴더에 JSON dump + parser script.

---

## 🔑 시니어 리뷰가 잡은 Critical 결함 (모두 v2 반영됨)

### U-01 Splash
- 서버 측 `auth()` redirect로 FOUC 방지 (클라 redirect만으로는 race)
- `<Link prefetch>` 활용 (router.push 비권장)
- `onPress` → `onClick` (RN 잔재 제거)
- `next/font/local` Pretendard, `export const viewport` 명시
- Figma device frame(420×844 stroke=#aaa radius=24)은 mockup canvas — 화면 요소 아님
- WCAG 대비비 명시
- v7 토큰 매핑 미정 항목들을 "디자이너 결정" 섹션으로 분리

### U-02 회원가입 공통
- ⚠️ **보안 옵션 A 채택** — password는 React state 메모리에만, sessionStorage 미저장. 새로고침 시 1단계 redirect.
- ⚠️ **Idempotency-Key** — 1단계 진입 시 uuid v4 생성 → POST signup header. 서버 1시간 캐시.
- ⚠️ **자동 로그인 race condition 방지** — `signIn(..., { redirect: false }) + router.replace` 금지. `signIn(..., { redirect: true, callbackUrl })` 사용. sessionStorage 정리는 `/user/home?from=signup` 진입 시점에.
- middleware vs server-side auth 책임 분리 명시
- ICU plural (7단계 카운터)
- vi 카피 일괄 fallback to en
- `healthy`/`omnivore` mutex = **데이터-레벨** (시각 자유, 저장 시 다른 옵션 무시 + 토스트)
- `night_owl`+`early_bird` 동시 허용 = **"시간대 무관" 시그널** (알고리즘 매핑 명시)
- alertdialog focus trap (Radix `<AlertDialog>` / react-aria 권장)
- OptionCard 시각 indicator `aria-hidden="true"` + 카드 자체에 role 부여
- 분석 이벤트(funnel) 정의

---

## 🎨 디자이너 결정 필요 (해소 후 description 갱신)

각 페이지 내 "디자이너 결정 필요" 섹션에 체크리스트로 박혀 있음. 주요:

- **OptionCard selected state** Figma에 없음 (v7 Iris 가설로 작성)
- **단계별 StepIcon SVG 6개** placeholder
- **에러 상태 컬러** (input 빨강 / Primary disabled 회색)
- **Primary 버튼 색** `#111827` vs Iris `#5B5FE0` (Splash와 통일)
- **ProgressBar fill 색** 동일
- **"권장 안 함" `#9ca3af`** WCAG AA 미달 → 강화
- **SNS 4개 제공자** (Google/Kakao/Apple/Naver?)
- **약관 본문 표시** (모달/별도 페이지/외부 링크)
- **2단계 `healthy` mutex 시각 UI** (데이터-레벨로 결정 — 시각은?)
- **4단계 알레르기 확인 다이얼로그 카피·시안** (Figma 없음)
- **`/user/login` 화면 정의** (이번 8개 밖)

---

## 📝 향후 별도 description 필요

본 8개 외에 추가로 정의가 필요한 화면들 (시니어 리뷰에서 식별):
- **U-02-1-terms** — 약관 본문 화면 (모달/페이지)
- **U-02-SNS** — SNS 가입 흐름 (password 없음 → 즉시 임시 계정 생성 + 인증된 PATCH)
- **U-02-1-login** — 로그인 화면 (Splash에서 "이미 계정이 있으신가요? 로그인" 이동)

---

## 🗂️ 다음 세션 시작점

1. **디자이너 결정 사항 해소** — 위 §디자이너 결정 필요 표 항목들 결정 → 각 description 페이지 v2 patch
2. **별도 description 작성** — terms / SNS / login 3개 + 메인 화면 (U-03) + 마이페이지 (U-12) + 알림 (U-04) — A 도메인 잔여
3. **U-03 메인 화면** — v7 6 시점 surface 적용 첫 화면. 시점 자동 전환 로직 description 필요.
4. **세로 슬라이스 코딩** — A 도메인은 `web/src/app/user/` 폴더에 description 따라 구현. (작업자 = ClaudeCode + 사용자 검수)

---

## 🔗 활성 참조

- **인덱스**: [Life OS ONE_v2](https://www.notion.so/35dc2986e14081a2b4b9dd2b1ddcc486)
- **부모**: [화면 Description](https://www.notion.so/35fc2986e14081cc9890efbd525ab46d) ← 8개 description 다 여기서 navigation
- **기획서 v5**: [📘 기획서 v5_20260512](https://www.notion.so/35ec2986e1408111972bd8a7b16c8c0c)
- **유저 플로우**: [유저 플로우 v1 — 라이언](https://www.notion.so/35ec2986e140819fb2f3cbfc36318f41)
- **디자인 가이드 v7**: [Cool Surface + Atom Voice](https://www.notion.so/35ec2986e140817eae13e458c3d21fa4) — 상세는 [GitHub Pages](https://zjoy8841.github.io/loo/docs/design-guide/design-guide.html)
- **Figma 파일**: `7gay0y94AxMz4Q82dMSdI7` (LIFE OS ONE_사용자 APP)

---

## 🧹 노션 부모 인덱스 등재 (다음 세션 작업)

8개 description 모두 v1 완료 — 다음 세션에서 Life OS ONE_v2 인덱스의 **Active Policy Documents 표**에 `화면 Description` 행 추가 권장.
