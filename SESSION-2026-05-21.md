# 세션 노트 — 2026-05-21

> 어제(05-20) 회원가입 v1 + Postgres + Vercel 배포 완료 직후 → 오늘 새벽까지 이어진 작업.

## 요약

스플래시·메인·결제·알림 4개 도메인을 description v1 / Figma 시안에 정합한 **v0.1 그레이스케일** 구현 일괄 완료. D-day(2026-05-29) 기준 골든 패스(회원가입→메인 active→예약하고 결제→결제 확인→예약 상태→예약 상세)와 알림 흐름(메인 종→센터→도착→결과 상세→예약 상세) 동작.

## 완료 작업

### 1. 스플래시 (U-01) Figma `1:211` 정합
- emerald 톤 → 그레이스케일 (signup-accent 토큰 공유)
- 라이언 마스코트 96px + h1 "라이언" + 부제 "AI 비서와 함께 일상의 AX"
- 특징 카드 3개 (Sparkles · Sunrise · MapPin lucide 라인 아이콘)
- i18n ko/en/vi (vi → en fallback)
- 위치: `web/src/app/user/page.tsx`

### 2. 메인 (U-03) active/idle 분기 v0.1
- mode state 분기 (`?mode=idle` query 또는 chat 입력 시 active 전환)
- **active**: Header(햄버거/라이언/종+배지) + 인사 + 페르소나 칩 + Hero Decision Card(닭가슴살 샐러드 mock) + Detail Grid 3(RESTAURANT/NUTRITION/NEARBY) + 인사이트 한 줄
- **idle**: 시점 라벨(점심) + 인사 + 서브헤드 + 마스코트 + 페르소나 칩 + 빠른 액션 칩 6개(가로 스크롤 + scrollbar-hide)
- ChatInputBar sticky bottom (첨부 / 텍스트 / 마이크 / 전송 4슬롯)
- 종 → /user/notifications wiring
- Hero CTA "예약하고 결제" → /user/order wiring
- 위치: `web/src/app/user/home/page.tsx` + `web/src/components/home/*` (HomeView·HomeHeader·HomeActive·HomeIdle·ChatInputBar)

### 3. 단건 결제 (U-05) v0.1 — 5화면
- **U-05-1** `/user/order` — LocationBar + RyanBox(reason) + MenuHero + MenuInfo + 메뉴 구성 + 식당 정보 4행 + SlotGrid(오늘 4 슬롯, 13:00 마감) + 합계
- **U-05-2** `/user/order/checkout` — ReservationSummary + RefundPolicyBox + PayMethodList(토스/카카오/네이버/삼성) + TermsList + MerchantAcceptanceNotice + PG 오버레이(mock 2초 후 status)
- **U-05-3** `/user/order/status` — StateHero 3 phase(waiting/confirmed/rejected). waiting → 3초 후 confirmed 자동 전환 (mock 데모용)
- **U-05-4** `/user/order/failed` — 2 variant(method/context). 슬롯 hold 박스 + 결제 시도 내역
- **U-05-5** `/user/reservation/[id]` — 5 status hero + 액션 매트릭스(길찾기/식당연락/일정변경/예약취소) + 환불 정책 박스 + 활동 타임라인
- 공통 컴포넌트: `web/src/components/payment/PaymentHeader.tsx` · `RyanBox.tsx` (variant: reason/wait/promise/alternative)

### 4. 알림 (U-04) v0.1 — 8화면
- **U-04-1 센터** `/user/notifications` — Filter chip(전체/이벤트/일정/시스템) + 미확인/최근 섹션 + NotificationCard
- **U-04-2 도착** `/user/notifications/[id]` — kind 아이콘 + 헤드라인 + 본문 + RyanBox + 처리하기/대화 CTA
- **U-04-3 일정 타임라인** `/user/schedule` — RuleCard + Toggle switch + 편집 링크 + "+ 규칙 추가"
- **U-04-4 폼** `/user/schedule/new` — 제목 + 반복 패턴 4종 + 시각 + 조건/행동 필드 + 편집 모드 지원(`?rule=...`)
- **U-04-5 설정** `/user/notifications/settings` — kind별 push/inapp 토글 + 조용한 시간
- **U-04-6 결과 상세** `/user/notifications/[id]/result` — 요약 + 관련 예약/플랜 + RyanBox(이 알림 왜 도착)
- **U-04-7 대화** `/user/notifications/chat` — Ryan ↔ me 메시지 로그 + 자주 묻는 규칙 칩 + ChatInputBar + mock 응답 후 플랜 풀뷰 진입
- **U-04-8 플랜 풀뷰** `/user/plan/[id]` — 플랜 헤더 + 진행 단계 타임라인 + 편집 CTA
- mock 데이터: `web/src/lib/notifications/mock.ts` (NOTIFICATIONS 5건 + SCHEDULE_RULES 3건)

### 5. i18n 신규 네임스페이스
- `user.splash.*`
- `user.home.*`
- `user.lunch_payment.*`
- `user.notification.*`
- 모두 ko/en. vi는 en fallback 정책 유지.

### 6. 인프라
- `globals.css`에 `.scrollbar-hide` utility 추가 (가로 스크롤 칩 영역)
- AIVoiceFAB 도메인별 숨김 정책: 회원가입·결제·예약·알림·일정·플랜·메인(ChatInputBar 마이크 있음)
- `.claude/settings.local.json` 권한 확장 (자동 승인 모드)

## 코드량 (대략)

- 신규 파일: 20여 개 (page.tsx 14 + 컴포넌트 4 + mock 1 + 세션 노트)
- 신규 라인: ~4,500줄
- i18n ko/en 추가 키: ~150건

## 다음 세션 (2026-05-22+)

1. 골든 패스 점검 — 회원가입 → 메인 active → 예약·결제 → 예약 상태 → 예약 상세 풀패스 brower-driven 검증
2. 개발 가이드 4종 작성 (아키텍처·DB·배포·커밋·보안)
3. 사이드 패널 v0.2 항목 확정 (사용자 결정 대기)
4. 로그인 화면 description + 구현
5. 시점 시나리오 U-06·U-07·U-10
6. emoji → line icon 잔여 화면 (lunch/voice/morning/evening/map)

## 알려진 한계

- 결제 PG 핸드오프: mock 2초 후 성공만 시연. 실제 PG SDK·실패·webview·timeout 미구현
- 알림 푸시: 인앱 mock만. 실제 push notification·polling 미구현
- 메인 사이드 패널: alert placeholder. v0.2 항목 미정
- 라이언 LLM 응답: mock 정적 텍스트. /api/chat 미연결
- 회원가입 1단계 완료 후 메인 직접 도달 (2~7단계 건너뜀 가능). 라우팅 가드 v0.2

## 풀패스 시각 검증 (2026-05-21 추가)

21개 화면 Playwright 브라우저 클릭 + 캡처:

- **골든 패스 클릭 흐름** ✓ — 스플래시 → 메인 active → 예약하고 결제 → 결제 확인 → ₩9,800 결제하기 → PG 오버레이(2초) → 예약 상태 waiting → (3초 자동) confirmed → 예약 상세로 → 예약 상세 confirmed
- **결제** ✓ menu / checkout / status 3 phase (waiting·confirmed·rejected) / failed 2 variant (method·context) / reservation 5 status (waiting·confirmed·completed·rejected·cancelled)
- **알림** ✓ 센터 / 도착 / 결과 상세 2종 (event reservation 연결·schedule plan 연결) / 일정 타임라인 / 규칙 폼 / 설정 / 라이언과 대화 / 플랜 풀뷰

### 발견 + 처리

| # | 이슈 | 처리 |
|---|---|---|
| 1 | "₩₩9,800 결제하기" — i18n 키 prefix와 won() 헬퍼 중복 | i18n `lunch_payment.cta.pay`에서 ₩ 제거 (commit 4789f9d) |
| 2 | proxy.ts PROTECTED_ROUTES에 결제·예약·알림·일정·플랜 미보호 | 4개 라우트 추가 (commit 4789f9d) |
| 3 | 메인 인사이트 "다가오는14:00…" 공백 약함 | `mr-1` → `mr-1.5` |

## 디자인 정합 노트

- Figma SSoT 우선 (Figma > 노션 > git) — 스플래시·메인·결제는 Figma 시안 정합. 알림은 Figma node TBD라 노션 description + wireframe HTML 기반
- 그레이스케일 + signup-accent 토큰 swap-ready (디자인 컨펌 시 `globals.css` 한 곳 swap)

---

## 추가 작업 (2026-05-21 오전~오후)

### 1. prod 빌드 통과 (`bde500f`)
어제 풀패스 검증이 dev 환경 기반이라 prod `next build` 미검증. 돌려보니 `useSearchParams()` Suspense 경계 누락으로 5개 페이지 prerender 실패 (`/user/order/{checkout,status,failed}`, `/user/schedule/new`, `/user/reservation/[id]`). default export를 `<Suspense fallback={null}>` wrapper로 분리, 본문은 `*Content` 함수로 추출. lint warning 4건도 함께 정리.

### 2. 메인 추천 분기 시스템 (`46701a5` → `7632406`)
청중이 회원가입 7단계 입력해도 메인이 모두 동일 화면이라는 본질 문제 해결. 정적 분기 추천 도입.

- `web/src/lib/recommendation/{menus,recommend}.ts` 신규
- 메뉴 mock 풀 16종 (잡식·채식·비건·페스코·할랄 분포 + healthFit/allergens 메타)
- `pickMenu(profile, seed)` 우선순위: allergyTags 제외 → dietTags 호환 → healthTags 매칭 점수
- 잡식 청중 동점 수렴 문제 해결: user.id seed 기반 deterministic 동점 깨기 (같은 user는 항상 같은 메뉴, 다른 user끼리 자동 분산)
- 페르소나 칩 동적 생성 (healthTags 라벨 + dietTags 보강, healthy/omnivore는 칩에서 제외)

### 3. UX 보강 (`bc9ba77`)
- "다른 추천" 버튼 활성화 — candidates 상위 6개 순환, 후보 1개뿐이면 disabled
- 회원가입 미완료 자동 회수 — `/user/home` 진입 시 Profile 전체 비어있으면 `/user/signup/health`로 redirect

### 4. 시연 흐름 개편 (`2f2db33`)
메인 default를 idle로 변경 + idle에 라이언 알림 인터럽트 인터랙션.

```
회원가입 완료 → idle(라이언과 대화창) → 1.5초 후 알림 카드 슬라이드인
   → 청중 클릭 → active(Hero Decision Card, 본인 데이터 기반 메뉴)
   → 결제 풀패스
```

description의 active = "알림 인터럽트" 의미와도 정합. 청중 임팩트: "내 데이터가 반영된 추천을 알림으로 받았다" 동선.

## 4 케이스 풀패스 브라우저 검증 (Playwright)

회원가입 7단계 풀패스 클릭으로 메인 분기 검증:

| Case | 입력 핵심 | 메뉴 | 페르소나 칩 |
|---|---|---|---|
| A | 콜레스테롤·잡식·다이어트 | 연어 포케 → "다른추천" → 등푸른 생선구이 | 콜레스테롤 걱정중 |
| B | 혈당·healthy·비건·견과알레르기 | 케일·퀴노아 그레인볼 | 혈당 관리중 / 비건 |
| C | 고혈압·페스코·벌크업 | 토마토 페투치네 | 고혈압 투약중 / 페스코 |
| D | 콜레+피로·잡식·갑각류알레르기 | 연어 포케 | 콜레스테롤 걱정중 / 만성 피곤러 |

검증: 페르소나 칩 동적 생성 ✓ / allergyTags 회피 ✓ (B 아사이볼·D 쉬림프 제외) / dietTags 호환 ✓ / healthTags 매칭 ✓ / seed 동점 분기 ✓ / "다른 추천" 순환 ✓

테스트 user 4명 prod DB cleanup 완료.

## 발견 사항 — 알림 Figma node 매핑 누락

`FIGMA-MANIFEST.md`에 알림 U-04 8화면 모두 `_TBD_`로 적혀 있었지만, 실제로는 Figma `알림_v1` 페이지(`23:195`)에 7 frame 이미 존재:

| description | Figma frame | node-id |
|---|---|---|
| U-04-1 센터 | `알림` | `23:231` |
| U-04-2 도착 | `알림도착` | `29:6042` |
| U-04-3 타임라인 | `마이페이지-알림` (추정) | `23:310` |
| U-04-4 폼 | Figma 없음 추정 | — |
| U-04-5 설정 | `마이페이지-알림 설정` | `29:6285` |
| U-04-6 결과 상세 | `결과 상세` | `29:6528` |
| U-04-7 대화 | Figma 없음 추정 | — |
| U-04-8 플랜 풀뷰 | `플랜 상세` | `30:7220` |

→ 어제 알림 8화면 구현이 wireframe + description 기반이었음 (Figma SSoT 정책 위반). 매니페스트 갱신·정합 작업은 시연 후 v0.2 우선순위로 보류.

## 그 외 발견

- NextAuth signOut을 GUI confirmation 단계 없이 POST + csrfToken으로 처리 (Playwright automation에 유용)
- proxy.ts stale 쿠키 무한 redirect 문제: 쿠키 존재만 검사하는 정책 — DB에서 user 삭제됐는데 쿠키만 남아있으면 home → 스플래시 → home loop. 시연 직전 DevTools 쿠키 클리어 또는 시크릿창 안내 필요. v0.2에서 NextAuth 콜백에 user 존재 검증 고려.

## 다음 세션 (2026-05-22+) — 갱신

기존 6개 + 신규 3개:

7. **알림 도메인 Figma 정합** — 매니페스트 `_TBD_` 채우기 + 6개 frame fetch + 코드 vs Figma 시각 차이 검토
8. **stale 쿠키 자동 정리** — NextAuth 콜백 또는 proxy에 user 존재 검증
9. **메인 idle ↔ active 토글 UI** — URL query 외 자연스러운 진입

---

## 추가 작업 (2026-05-21 야간 무인 — Claude 자율 진행)

### 5. 회원가입 7화면 Figma 정합 (`d28b502`)

사용자가 자러 가기 전 "회원가입을 한 다음에 메인 콘텐츠를 경험" 흐름 점검 중 발견 — 회원가입 7화면이 Figma 시안과 어긋남. 매니페스트 node-id는 채워져 있었지만(`2:463`~`2:915`), 어제 c863a84 작업이 description + 부분 Figma 정합만 거침. 사용자 결정: (a) 1단계 라이언 마스코트 유지(code-extension), (b) 이메일 debounce 중복체크 구현 후 Figma에 사용자가 직접 추가.

**Figma fetch + 시각 정합 적용 항목**:

- **OptionCard split layout** — 좌상단 원형 아이콘 outline + 우상단 multi/single 인디케이터(rounded-[4px]/rounded-full) + 하단 라벨. 카드 min-h 110px. 선택 시 indicator에 Check 아이콘.
- **StepIntro ModeChip** — 단순 paragraph → 둥근 outline badge (bg-gray-100 border-gray-300, "□ 복수 선택 가능" / "○ 1개만 선택").
- **BottomActions LaterButton** — "건너뛰기" → "나중에 입력" + "권장 안 함" subtext 2줄.
- **enums.ts SHAPE_GOALS.maintain** — "그냥 유지만 할래요" → "유지어터" (Figma 톤 정합).
- **1단계 (account)**:
  - 이메일 debounce 중복체크 — `/api/auth/check-email` 신규 + useEffect 500ms + state badge(확인 중/사용 가능/중복) + 중복 시 "로그인하기" 링크. **React 19 set-state-in-effect 규칙** 따라 localStatus derive + remoteResult만 state.
  - 비밀번호 조건 체크리스트 — 8자/영문/숫자/특수문자, Check/X 아이콘.
  - SNS 4버튼 — "K/N/G" 텍스트 → 원형 outline 안 inline SVG (카카오 chat bubble, 네이버 N, 구글 +, Apple logo). 그레이스케일 유지.
  - placeholder · 강도 라벨 카피 정합.
- **4단계 (diet)**: 알레르기 0개일 때 경고 박스 시각 표시 — "건너뛰기 비권장 — 알레르기 정보 누락 시 추천 안전성 보장 불가" (Figma의 alert dialog 전 단계).

**Figma `2:463` 안 별도 state variant frame**으로 발견된 텍스트(`사용 가능`/`중복`/`이미 가입된 이메일이에요 · 로그인하기`/`debounce 500ms`)가 사용자가 "Figma에 없다"고 한 디테일이었음. 시각 매핑 완료, 사용자가 Figma에 직접 정리 예정.

### 6. Playwright 시각 검증 (1단계만)

- 비밀번호 `Test1234!` 입력 → 조건 4개 모두 ✓
- 이메일 `fresh-test-...@example.com` → "사용 가능" 배지 (debounce 500ms 후)
- 이메일 `hangain@naver.com` (DB 기존 user) → "중복" 배지 + "이미 가입된 이메일이에요 · 로그인하기" 링크

2~7단계는 풀패스 클릭 회피 (prod DB user 오염 방지). 공통 컴포넌트 변경이라 1단계 시각만 검증해도 다른 단계도 동일 적용 추정. 사용자가 시연 시뮬레이션 시 직접 풀패스 확인.

### 7. 무인 작업 환경 설정 (`.claude/settings.local.json`)

사용자 명시 요청으로 `permissions.defaultMode: "bypassPermissions"` + `skipDangerousModePermissionPrompt: true` 적용. 다음 세션에서 일반 모드로 복귀하려면 두 라인 제거 또는 `defaultMode: "default"`로 변경.

## 빌드 누적 (오늘 마지막 push)

| commit | 내용 |
|---|---|
| `bde500f` | fix(build): Suspense 5건 + lint cleanup |
| `46701a5` | feat(home): 회원가입 데이터 기반 추천 + 페르소나 칩 |
| `3cc658e` | feat(home): 메뉴 풀 16종 |
| `7632406` | fix(home): seed 동점 분기 |
| `bc9ba77` | feat(home): 다른 추천 + 회원가입 가드 |
| `2f2db33` | feat(home): idle default + 알림 인터럽트 |
| `5886737` | docs(session): 오전·오후 정리 |
| `d28b502` | feat(signup): U-02 회원가입 7화면 Figma 정합 |

빌드 35/35, typecheck/lint 0.
