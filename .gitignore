# OS
.DS_Store
Thumbs.db

# IDE / Editor
.idea/
.vscode/
*.swp
*~

# Claude — local-only settings (settings.local.json은 머신마다 다름, 하위 폴더 포함)
**/.claude/settings.local.json

# Playwright MCP 산출물 (도구 캐시 — 공유 가치 없음)
.playwright-mcp/

# Node / Next.js (web/.gitignore 와 중첩 적용)
node_modules/
.next/
out/
build/
coverage/
*.tsbuildinfo
next-env.d.ts

# 환경변수 (절대 커밋 금지 — .env.example 만 커밋)
.env
.env.local
.env.*.local

# 로그
*.log
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*

# Vercel
.vercel

# Foundation 원본 (노션이 단일 진실 공급원 — 큰 binary 제외)
Life OS ONE.pdf
Life OS ONE.txt
Life OS ONE_utf8.txt
KakaoTalk_*.mp4

# 로컬 작업 자료 (개인 메모·스크린샷·다운로드 레퍼런스 — 공유 가치 낮음)
\[스크린샷\] LIFEOSONE/
회의록/
자료/
